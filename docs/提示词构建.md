# 提示词构建链路

> 审查日期：2026-07-20。范围：`backend/` 内 LLM / 出图相关提示词。  
> 风格：规则字符串硬编码 + `f-string` / `.format()` 拼装，无外部模板引擎。  
> 相关：`docs/LLM.md`（thinking 开关）、`docs/选题.md`（选题策略）。

---

## 总览

```text
job / 配置参数
  → build_*_prompts() 拼 system + user
  → llm_mgr / DeepSeekClient
  → 解析 JSON / 字符串
  → wrap / normalize / LoRA / 合规（可选）
  → 落库或调用出图 / I2V API
```

预览汇总入口：`script/compose.collect_prompts`（写入
`script["llm_prompts"]`）。

统一包装：`prompt_common.prompt_step` →
`{step, label, system, user}`。

---

## 链路索引

| 编号 | 名称 | 构建入口 |
| --- | --- | --- |
| A1 | 口播文案 | `voiceover_standard.build_voiceover_standard_prompts` |
| A2 | 画面概述 | `visual_brief.build_visual_brief_prompts` |
| A3 | 文生图/动效 | `image_prompt.build_image_prompts` |
| A4 | 口播扩写 | `voiceover_standard.build_voiceover_standard_expand_prompts` |
| A5 | 单段缩句 | `voiceover_standard.build_voiceover_standard_shrink_prompts` |
| B1 | 素材口播 | `voiceover_material.build_voiceover_material_prompts` |
| B2 | 视频帧分析 | `video_analyzer._build_prompts` |
| C1 | 批量选题 | `topic/prompts/builder.build_topic_*` |
| C2 | 选题优化 | `build_topic_optimize_*` |
| D1 | 日常主题 | `build_daily_story_theme_prompts` |
| D2 | 日常故事 | `build_daily_story_prompts` |
| D3 | 日常分镜 | `build_daily_script_prompts` |
| D4 | 对话标题 | `build_chat_title_prompts` |
| E1 | 标题优化 | `optimize_title.build_title_optimize_*` |
| E2 | 视频简介 | `description.build_video_description_*` |
| E3 | 标签 | `tags.build_tags_*` |
| E4 | Pixabay 改写 | `query_rewrite_prompts.build_pixabay_*` |
| E5 | 封面文生图 | `cover_layout.build_cover_image_prompt` |
| F1 | SD15 英文化 | `image_sd15.build_sd15_prompt_*` |
| F2 | SD15 最终串 | `image_sd15.build_sd15_full_prompt` |
| F3 | Agnes 校验 | `image_agnes._VERIFY_SYSTEM_PROMPT` |
| F4 | I2V 动效 | `video_wan` / `video_agnes` |

非内容生成（不列入主链路）：

- `TTS_INSTRUCTION`：CosyVoice instruct，配置项
- `quality/*`：规则质检，不拼 LLM prompt

---

## A. 标准脚本流水线

规则与拼装在：`backend/app/services/script/`。

| 路径 | 内容 |
| --- | --- |
| `prompt_common.py` | 共用辅助（profile、字数预算、`prompt_step`） |
| `voiceover_standard/` | 自由口播 A1/A4/A5；`styles/` 按风格拆规则 |
| `voiceover_standard/styles/common.py` | 跨风格共用规则与 JSON 样例 |
| `visual_brief.py` | visual_brief 规则与 A2 构建 |
| `image_prompt.py` | 六维规则、motion、A3、`wrap_image_prompts` |
| `segment_split.py` | 口播切分镜 `apply_segments_from_voiceover` |
| `voiceover_material/` | 素材口播 B1 构建 |

运行时：`llm_deepseek` → worker
`stages/standard/script.py`。

`visual_style` **不由 LLM 生成**，在
`llm_deepseek._resolve_visual_style` 按
`content_style` 硬编码。

### A1 口播文案

| 项 | 说明 |
| --- | --- |
| 入口 | `build_voiceover_standard_prompts` |
| 调用 | `DeepSeekClient._generate_narration` |
| 注入 | title、style、orientation、字数、补充信息、feedback |
| 产出 | `title`、`narration`、`word_count`（无 segments） |
| 后续 | `segment_split.apply_segments_from_voiceover` |

### A2 画面概述

| 项 | 说明 |
| --- | --- |
| 入口 | `build_visual_brief_prompts` |
| 调用 | `DeepSeekClient._fill_visual_briefs` |
| 注入 | 已切分 segments 的 text、全文 narration、visual_style、title |
| 产出 | 每段 `visual_brief`（不改 text） |
| 约束 | 妈妈角色仅在有妈妈台词的镜出现 |

### A3 文生图 / 动效提示词

| 项 | 说明 |
| --- | --- |
| 入口 | `build_image_prompts` |
| 调用 | `fill_image_prompts`；API 亦可直接预览 |
| 规则选择 | `image_prompt_rule(orientation, content_style, sd15_mode)` |
| SD15 字段 | provider=`sd15_t2i` 时带 `sd15_prompt_en` |
| 分批 | `LLM_IMAGE_PROMPT_BATCH_SIZE` |
| 后处理 | `wrap_image_prompts`（如 daily_story 加参考图前缀） |
| 产出 | `image_prompt`、`motion_prompt`、可选 `sd15_prompt_en` |
| 落库 | `video_segment` 对应列 |

出图消费：`image_mgr.generate_segment_images`。

- SD15：优先 `sd15_prompt_en`
- 其它：`image_prompt`
- 含地图关键词时前置合规句

### A4 口播扩写

| 项 | 说明 |
| --- | --- |
| 入口 | `build_voiceover_standard_expand_prompts` |
| 时机 | 口播字数低于下限时 |
| 调用 | `llm_deepseek` 扩写重试 |

### A5 单段缩句

| 项 | 说明 |
| --- | --- |
| 入口 | `build_voiceover_standard_shrink_prompts` |
| 时机 | 单镜口播过长 |
| 调用 | `shrink_segment_texts` |

---

## B. 素材管线

### B1 素材口播

| 项 | 说明 |
| --- | --- |
| 入口 | `build_voiceover_material_prompts` |
| 时间表 | `board_timeline.timeline_*_for_prompt` |
| 调用 | `generate_material_script`；`collect_prompts` 在 material job 走此步 |
| 产出 | 对齐视频时间轴的口播脚本 |

### B2 视频帧分析

| 项 | 说明 |
| --- | --- |
| 入口 | `material/video_analyzer.VideoAnalyzer._build_prompts` |
| 输入 | 抽帧 base64 + 间隔秒数 |
| 产出 | 物品/对象时间表 JSON（title / segments） |

---

## C. 选题

目录：`backend/app/services/topic/prompts/`。

| 文件 | 内容 |
| --- | --- |
| `builder.py` | system/user 拼装 |
| `common.py` | 合规与 hook 规则 |
| `format.py` | JSON 输出格式 |
| `../catalog.py` | 模板定义（误区反问式等） |

### C1 批量选题

| 项 | 说明 |
| --- | --- |
| 入口 | `build_topic_system_prompt` / `build_topic_user_prompt` |
| 调用 | `topic_mgr.generate_topics` → `llm_mgr.generate_topics` |
| 可覆盖 | API 可传入自定义 `system_prompt` / `user_prompt` |
| 产出 | topics 列表（title / category / template / hook） |

### C2 选题优化

| 项 | 说明 |
| --- | --- |
| 入口 | `build_topic_optimize_system_prompt` / `user` |
| 调用 | `topic_mgr.optimize` |
| 产出 | 单条优化后的 topic |

策略说明见 `docs/选题.md`。

---

## D. 日常故事

文件：`backend/app/services/daily_story/prompts.py`。

角色外貌常量 `DAILY_STORY_CHARACTERS` 同时进入
`visual_style` 与 A3 的 wrap 前缀。

### D1 主题生成

- 入口：`build_daily_story_theme_prompts(count)`
- 产出：主题列表

### D2 故事正文

- 入口：`build_daily_story_prompts(theme)`
- 产出：故事内容（对话剧本原料）

### D3 分镜脚本

- 入口：`build_daily_script_prompts(...)`
- 调用：daily_story stage / `job_mgr`
- 产出：分镜脚本

### D4 对话标题

- 入口：`optimize_title.build_chat_title_prompts`
- 调用：`worker/stages/daily_story/script.py`
- 产出：适合对话体的标题

---

## E. 元数据 / 检索 / 封面

### E1 标题优化

- 入口：`script/optimize_title.py`
- 调用：脚本阶段末 / `collect_prompts`
- 注入：`draft_title`、`narration`、`max_title_length`

### E2 视频简介

- 入口：`script/description.py`
- 调用：`llm_deepseek.generate_video_description`

### E3 标签

- 入口：`script/tags.py`
- 调用：`llm_deepseek.generate_tags`

### E4 Pixabay 检索改写

- 入口：`clip_search/query_rewrite_prompts.py`
- 调用：`rewrite_pixabay_query`
- 产出：短检索词

### E5 封面文生图

- 入口：`intro/cover_layout.build_cover_image_prompt`
- 调用：intro stage / `job_mgr`
- 说明：直接拼出图字符串，非 LLM system/user

---

## F. 出图侧二次加工

脚本阶段产出的字符串，在真正调 API 前可能再改一次。

### F1 SD15 英文化

| 项 | 说明 |
| --- | --- |
| 入口 | `build_sd15_prompt_system` / `build_sd15_prompt_user` |
| 调用 | `llm_mgr.prepare_sd15_image_prompt` ← `_prepare_sd15_prompt` |
| 时机 | 非「已是英文短 prompt」快路径时 |
| 产出 | 英文 `prompt_en`、business、可选左右分镜 |

### F2 SD15 最终串

| 项 | 说明 |
| --- | --- |
| 入口 | `build_sd15_full_prompt` |
| 组成 | LoRA 前缀 + subject + business 后缀 |
| 配置 | `SD_BUSINESS`、checkpoint / LoRA 解析 |

### F3 Agnes 出图校验

| 项 | 说明 |
| --- | --- |
| 入口 | `image_agnes._VERIFY_SYSTEM_PROMPT` |
| 用途 | 校验生成图是否符合要求（非生图 prompt） |
| 生图 | Agnes 原样提交 `prompt` 字段 |

### F4 I2V 动效

| 项 | 说明 |
| --- | --- |
| 默认句 | `_DEFAULT_MOTION_PROMPT`（wan / agnes 各一份） |
| 稳定化 | `_stabilize_motion_prompt`；空则填默认 |
| 服务端扩写 | `WAN_I2V_PROMPT_EXTEND`（本地不再拼模板） |

T2I 侧同类开关：`WAN_PROMPT_EXTEND`、
`Z_IMAGE_PROMPT_EXTEND`。

---

## 配置项（与提示词相关）

| 配置键 | 作用 |
| --- | --- |
| `LLM_IMAGE_PROMPT_BATCH_SIZE` | A3 分批大小 |
| `IMAGE_PROVIDER` | 是否生成 `sd15_prompt_en`、出图路径 |
| `SD_BUSINESS` | SD15 business 覆盖 |
| `WAN_PROMPT_EXTEND` | 万相 T2I 服务端扩写 |
| `Z_IMAGE_PROMPT_EXTEND` | Z-Image 服务端扩写 |
| `WAN_I2V_PROMPT_EXTEND` | 万相 I2V 服务端扩写 |
| `MAX_TITLE_LENGTH` | 标题字数进 A1/C/E1 |
| `TTS_INSTRUCTION` | TTS instruct（非 LLM） |
| `SKIP_SCRIPT_QUALITY_CHECK` | 是否跳过 image_prompt 质检 |

任务级覆盖：`info.content_style`、`orientation`、
`supplementary_info`、`image_provider`。

---

## 改提示词该动哪

| 目标 | 优先改 |
| --- | --- |
| 风格规则文案 | `voiceover_standard/styles/*`（共用见 `common.py`） |
| 文生图规则 | `image_prompt.py` |
| 选题 / 日常 | `topic/prompts/*`、`daily_story/prompts.py` |
| 拼装 / 风格分支 | `prompt_common` + 各 builder |
| 调用与重试 | `llm_deepseek.py` |
| 出图最终串 | `wrap_image_prompts`、`build_sd15_full_prompt` |
| 预览步骤集合 | `compose.collect_prompts` |

---

## 标准主路径时序

```text
A1 voiceover_standard
  → segment_split（切 segments）
  → A2 visual_brief
  → A3 image_prompt (+ wrap)
  → E1 标题优化（可选）
  → 出图：image_prompt 或 F1→F2
  → I2V：motion_prompt + F4
```

素材主路径：`B2`（可选入库分析）→
`B1 voiceover_material` → 后续视 pipeline 接出图。

日常故事：`D1` → `D2` → `D3` → `D4`，文生图仍走
A3 规则中的 daily_story 分支与 wrap。
