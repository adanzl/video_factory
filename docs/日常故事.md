# 日常故事流水线

> 更新日期：2026-07-27。  
> 流水线 DB 值：`pipeline=chat`（常量名 `PIPELINE_DAILY_STORY`）。  
> **五类矛盾**见 [日常故事-类型.md](./日常故事-类型.md)。  
> 提示词索引见 [提示词构建.md](./提示词构建.md) §D。

---

## 1. 端到端

```text
主题 / 生成剧本（daily_story 表）
  → create_job（video_job, stage=script, pending）
  → script → tts → segment → intro → merge → publish
```

- 故事层：`DailyStoryMgr` + `api_daily_story`
- 视频层：`worker/stages/daily_story/*` + 共享
  `Segment` / `Intro` / `Merge` / `Publish`
- 建任务只落 `pending`，需在任务页手动跑阶段（无独立 cron）

关键代码：

- `backend/app/services/daily_story/story_types/`（五类线路）
- `backend/app/services/daily_story/prompts.py`（拼装与通用校验）
- `backend/app/services/daily_story/story_types/quality.py`（按类型观感路由）
- `backend/app/services/daily_story/quality.py`（观感总分）
- `backend/worker/stages/daily_story/{script,tts}.py`
- `backend/app/services/daily_story/daily_story_mgr.py`

---

## 2. 生成与质检（摘要）

- 正文硬卡字数、发现开场、重试与观感 ≥85：见
  [提示词构建.md](./提示词构建.md) §D2/D2b 与下文 §3。
- 类型锁定、拼装字段、硬卡路由：见
  [日常故事-类型.md](./日常故事-类型.md) §2–§5。

---

## 3. 观感评分标准

实现：`quality.py` + `story_types/quality.py` 及各类型 `*/quality.py`。
`story.quality`：`grade` / `score` / `summary` / `reasons`。

### 3.1 总分构成

| 部分 | 范围 | 含义 |
| --- | --- | --- |
| **结构分** | 0–**80**（上限） | 格式、类型收束形态、推进层数、节奏、硬伤扣分 |
| **好笑分** | 0–**20** | 一锤场面、收束扣原话、具体细节；硬伤压低好笑分 |
| **总分** | 0–100 | `min(100, 结构 + 好笑)`，并按下表封顶 |

#### 档位（与产品口径一致）

- **≤80**：仅结构达标或好笑不足（好笑 &lt; 5 时总分不超过 80）。
- **≥85**：好笑分 **≥ 5**（好笑达标）。
- **≥95**：好笑分 **≥ 15**（很好笑）；好笑 5–14 时总分 **≤ 94**。

`reasons` 中会附带 `结构NN`、`好笑MM` 便于排查。

### 3.2 结构分（≤80）主要项

- 基础分与跑题、`conflict_core`、发现开场、punchline 类型标签。
- 妈妈裁判/偏多台词、同人连说、甩妈/和解/耍赖/无破功软收等硬伤。
- 冲突推进层数（按类型 `layer_patterns` 分段命中）。
- 绕圈与中段复读（辩论词连击、管教口头禅复读等）。
- 类型末段 scorer，计入结构侧（规则见类型文档 §3）。

### 3.3 好笑分（0–20）主要项

- **一锤场面**：中段可画细节。
- **收束扣原话**：末段引话能在前文对上。
- **硬伤**：引话无出处、A 末四拍不完整、模板复读等，压低或清零好笑分。

修订提示：观感 cons 含「好笑不足」「无出处」「末四拍」时，
`build_quality_revision_hints` 会追加 **【好笑】** 段，并与
`StoryTypeLine` 的 closing/escalation hint 合并。

### 3.4 等级文案

| 分数 | grade |
| --- | --- |
| ≥ 70 | 好 |
| 45–69 | 中 |
| &lt; 45 | 偏弱 |

---

## 4. 待做（按优先级）

### P1 — 剧本质量（单冲突）✅

已落地 `conflict_core` + 提示词【单冲突】硬约束 +
校验拦截后半跑题 + 重试禁换冲突。发现开场分步生成。

### P2 — 分镜节奏验收

提示词已给目标镜数 +「转折特写不拆碎」，但无 QA。

### P3 — 分发钩子

封面与发布标题优化（亲子向）。

### 暂缓

音效、建任务自动开跑、`publish/done` 残留状态。

---

## 5. 默认参数速查

| 参数 | 默认 | 位置 |
| --- | --- | --- |
| `speech_chars_per_sec` | 3.6 | `job_info` / 建任务 UI |
| `phrase_gap_sec` | 0.2 | `tts.speaker_configs` |
| TTS 昭昭 / 灿灿 / 妈妈 | 1.15 / 1.30 / 1.20 | StageChatTts |
| `orientation` | landscape | `create_job` |
| `video_provider`（seg） | 特写→agnes_i2v | `seg.info` |
| BGM | `#4` Molecules，`-14` dB | `info.bgm` |

---

## 6. 相关文档

- [日常故事-类型.md](./日常故事-类型.md)
- [提示词构建.md](./提示词构建.md) §D
- [LLM.md](./LLM.md)
- [成本.md](./成本.md)
