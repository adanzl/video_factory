---
description: Markdown 写作规范，避免 markdownlint 报错
globs: "**/*.md"
alwaysApply: true
---

# Markdown / markdownlint 规范

编辑或新建 `.md` 文件时遵守以下规则，保存前自检 markdownlint。

## 表格（MD060）

- 分隔行使用 `| --- | --- |`（管道符两侧各留一空格）
- 禁止紧凑写法：`|------|------|`
- 表头与分隔行列数一致

```markdown
| 列 A | 列 B |
| --- | --- |
| 值 | 值 |
```

## 代码块（MD031）

- 围栏代码块（\`\`\`）前后各留**一空行**
- 列表项内嵌代码块：先写完列表引导句并空一行，再写围栏块，块后再空一行

## 标题（MD024 / MD025）

- 同级标题不重复；文档仅一个 H1
- 章节用 `##`，子节用 `###`

## 行宽（MD013）

- 正文、表格、块引用每行 ≤ 80 字符（含中文）
- 代码块内长命令用 `\` 折行，或拆成多行 heredoc
- URL 用角括号或缩短描述链接，勿在正文堆长 URL

## 链接与 URL（MD034）

- 裸 URL 用 `<https://...>` 或 `[描述](url)` 包裹

## 列表

- 有序/无序列表前后各空一行（MD032）
- 列表项内多段内容：段间空行，子列表缩进一致

## 中文技术文档习惯

- 路径、命令、参数用行内代码：\`SD_HOME\`、\`--lowvram\`
- 长 shell 片段用 \`\`\`bash 围栏，不用单行硬折
- 块引用 `>` 用于注意/警告，不在块内嵌套代码块

## 保存前检查

```bash
npx markdownlint-cli2 "docs/**/*.md"
```
